### Command And Control - Terraform

This is the base image for the command and control node for automated builds

It contains:-

  * Hashicorp Terraform

Note, no credentials are installed, your child nodes should inject credentials as required.
